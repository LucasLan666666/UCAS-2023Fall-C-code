文件结束符EOF
在Linux环境下， 一切皆文件
只有字符串常量占用空间：2字节（举例："a"第一个字节存"a"，第二个字节存"\0"。）
字符串长度：一个字符串占据字节个数
数组：存储一组相同类型数据，也是一个变量
传值特点：不改变实参
--n: n = n-1
存一个字符串可以用字符数组
%s作用：输出字符串
数组作为形参的时候，长度可省略
写数组大小的目的；防止越界（C语言不检查数组越界，程序是可以运行的），越界会导致缓冲区溢出的错误
国际标准规定：字符串末尾必须存一个'\0'
当前面的结果已经确定整个表达式真假时，不再进行后面的运算
把四个字节的数据存入一个字节的空间时，舍去高位
局部变量存在栈，全局变量存在全局数据区
全局变量的声明： extern 类型 变量名
ANSI标准中：字母包括大小写字母和下划线
main不是保留字->可以当局部变量用
short int：短整型，16位
short，long，long long后面可以不用跟int
size of是保留字（运算符）
一个常量由0开头表示八进制数，0x十六进制，0b二进制
scanf{"%s", mystring}中mystring不带&: 数组名表示数组的起始地址
'\0'不计入字符串长度
枚举类型 enum boolean {TRUE, FALSE} //定义一个名"boolean"的数据类型，有TRUE和FALSE两种取值（第一个值编码为0，第二个值编码为1，以此类推）
系统不会给定义的数据类型分配空间，但会给变量分配空间
变量使用规则：(1)所有变量必须在声明后引用; (2)必须初始化(定义时或第一次引用时)
如果初始化一个数组的时候给出的值少于数组长度，则后面的都是0
逻辑运算符注意：(1)从左到右求值; (2)若在求值的过程中已经知道整个表达式的真假后，则立即停止
if(valid) <=> if(valid != 0) (如果一个变量不为零，条件判断时将其视为"真")
if(!valid)<=> IF(valid == 0)
强制类型转换
按位右移的时候，无符号数高位补0，有符号数高位补1
右移一位相当于除以2，左移一位相当于乘以2
x & 0177700 <=> 有"1"的地方不变，其余部分清零
expr_1? expr_2: expr_3（若expr1为真，结果为expr2；否则，结果为expr3）
逗号表达式：从左往右，赋值的时候最后一个是表达式的值，优先级别是运算符最低
isspace判断是否换行空格制表符
ACM: 美国计算机学会
SHELL排序基本思想: 先进行粗粒度排序, 再进行细粒度排序
循环套循环称为嵌套循环
逗号也是一个表达式
标识符后面加冒号表示标号
gcc -g -o filename f1.c f2.c f3.c ...将多个c文件编译生成同一个可执行文件
在多个文件出现同名全局变量, 就是同一个变量.
树 父亲 孩子(左右) 二叉树 子树(左右) 叶子
二叉树三种遍历方法: 先序遍历, 中序遍历(左根右, 得到中缀表达式), 后序遍历(左右根, 得到后缀表达式
push(压栈), pop(退栈), sp(指针), 系统栈, 堆栈特点: 先进后出
逆波兰表达式注意: 1. pop减数而非被减数 2. 注意被除数不为零
全局变量可以在任何地方声明(函数内可以声明全局变量, 用extern)
局部变量可以和全局变量同名, 但不是同一个变量, 对局部变量修改不会改变全局变量的值
自己定义的头文件要用引号括起来
static定义静态局部变量, 只会初始化一次, 函数退出时不会回收空间
